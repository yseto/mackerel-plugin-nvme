builds:
  - binary: mackerel-plugin-nvme
    goos:
      - linux
    goarch:
      - amd64
      - arm64
archives:
  - format: zip
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
release:
  github:
    owner: yseto
    name: mackerel-plugin-nvme

